import React from 'react';
import '../Header.css'; // Si tu veux ajouter un fichier CSS spécifique

const Header = () => {
  return (
    <header className="header">
      <h1>HRnet</h1>
    </header>
  );
};

export default Header;
